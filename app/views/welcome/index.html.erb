<h1>Welcome!</h1>

<% if current_user %>
  <p>Hello, <%= current_user.name %></p>
  <% @tweets.each do |tweet| %>
    <%= tweet.text %>
  <% end %>
  <%= link_to "Logout", logout_path %>
<% else %>
  <%= link_to "Login", login_path %>
<% end %>
